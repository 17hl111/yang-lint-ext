# ------------------------------------------------------------
# 基础设置：默认所有文本文件开启行尾规范化
# ------------------------------------------------------------
* text=auto

# ------------------------------------------------------------
# 源代码文件：统一 LF
# ------------------------------------------------------------
*.ts   text eol=lf
*.js   text eol=lf
*.json text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf
*.md   text eol=lf

# ------------------------------------------------------------
# Shell 脚本：必须 LF，否则在 Linux/Mac 容器里会报错
# ------------------------------------------------------------
*.sh   text eol=lf

# ------------------------------------------------------------
# Windows 批处理脚本：必须 CRLF，否则 Win 下执行不了
# ------------------------------------------------------------
*.bat  text eol=crlf
*.cmd  text eol=crlf

# ------------------------------------------------------------
# 二进制文件：禁止 Git 试图做文本 diff/合并
# ------------------------------------------------------------
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.pdf  binary
*.zip  binary
*.gz   binary
*.tgz  binary

# ------------------------------------------------------------
# 特殊情况：保留可执行权限（对 Linux/Mac 有效）
# ------------------------------------------------------------
# 例如扩展里的脚本
oas_generator/*.sh text eol=lf
